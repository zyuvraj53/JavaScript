// function setup() {

  // createCanvas(200,200);
  // background(50);
  // textSize(20);
  // noStroke();

  var words = RiTa.tokenize("Yes, it was me, you had thought correctly.")

  var r = new RiString("It was a dark and stormy night.");
  // for (let i=0; i < words.length; i++) {
  //     text(words[i], 50, 50 + i*20);
  // }
