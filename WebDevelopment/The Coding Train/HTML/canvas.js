function setup() {
  var graphics = createGraphics(600,400);
  createCanvas(400, 400);
  createP("My favourite colour is purple.");
}
function draw() {
  background(125, 0, 255);
  graphics.background(51);
}
